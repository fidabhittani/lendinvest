/**
 * Applications Middlwares
 * @param {*} store
 * @returns
 */

/**
 *  Handle Invest
 * @param {*} store
 * @returns
 */
export const handleInvest = (store) => (next) => (action) => {
  next(action);
};
